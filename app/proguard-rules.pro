# App-specific R8 rules. Keep this file short and documented — every rule needs a reason.
# kotlinx.serialization: serializers for @Serializable classes are looked up reflectively
# in some paths; the library ships consumer rules, so nothing extra is needed here yet.
