<?xml version="1.0" encoding="utf-8"?>
<!-- INVARIANT: no network permission, in any flavor. PictoSpeak is 100% offline
     (CLAUDE.md hard rule 1). Any PR adding a <uses-permission> for network is rejected. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".AacApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PictoSpeak">

        <!-- Required for Macrobenchmark measurement on physical devices (docs/perf-budgets.md). -->
        <profileable
            android:shell="true"
            tools:targetApi="29" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
