# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Kotlin
.kotlin/

# Local machine config (SDK path, signing) — never committed
local.properties
*.keystore
*.jks
keystore.properties

# IDE
.idea/
*.iml
.DS_Store

# Claude Code local overrides
.claude/settings.local.json

# Model weights are NEVER committed (see CLAUDE.md licensing map)
*.litertlm
*.task

# Claude Code runtime artifacts
.claude/
